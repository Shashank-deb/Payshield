services:
  db:
    image: postgres:16
    container_name: payshield-core-db-1
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      TZ: Asia/Kolkata
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB} -h localhost"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped
    networks: [payshield]

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: payshield-core-app-1
    environment:
      SPRING_PROFILES_ACTIVE: local
      SPRING_DATASOURCE_URL: ${DB_URL}
      SPRING_DATASOURCE_USERNAME: ${DB_USERNAME}
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD}
      SERVER_PORT: ${APP_PORT}
      JAVA_TOOL_OPTIONS: "-XX:+UseZGC -Xms256m -Xmx512m"
      TZ: Asia/Kolkata

      # Security Configuration
      SECURITY_JWT_SECRET: ${SECURITY_JWT_SECRET}
      SECURITY_JWT_TTL_SECONDS: ${SECURITY_JWT_TTL_SECONDS}

      # Bootstrap Admin
      BOOTSTRAP_ADMIN_EMAIL: ${BOOTSTRAP_ADMIN_EMAIL}
      BOOTSTRAP_ADMIN_PASSWORD: ${BOOTSTRAP_ADMIN_PASSWORD}

      # Actuator on separate management port
      MANAGEMENT_SERVER_PORT: ${ACTUATOR_PORT}
      MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: "health,info,prometheus"

      # Application Configuration
      STORAGE_BASE_PATH: ${STORAGE_BASE_PATH}
      OUTBOX_ENABLED: ${OUTBOX_ENABLED}
      OUTBOX_POLL_INTERVAL_MS: ${OUTBOX_POLL_INTERVAL_MS}
      OUTBOX_BATCH_SIZE: ${OUTBOX_BATCH_SIZE}

    ports:
      - "${APP_PORT}:${APP_PORT}"     # app
      - "${ACTUATOR_PORT}:${ACTUATOR_PORT}"   # actuator (published for dev/testing)
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    networks: [payshield]

volumes:
  pgdata:

networks:
  payshield:
    driver: bridge